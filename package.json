{
  "name": "justgiulio-photos-metadata",
  "version": "1.0.9",
  "type": "module",
  "description": "Automatic metadata generation for photo repository",
  "main": "scripts/generate-metadata.js",
  "scripts": {
    "generate": "node scripts/generate-metadata.js",
    "version": "node scripts/version.js"
  },
  "dependencies": {
    "sharp": "^0.34.4",
    "exiftool-vendored": "^28.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
